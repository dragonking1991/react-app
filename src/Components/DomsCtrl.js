// import { useEffect, useRef, useState } from "react";
// import { setEleStyle } from "../utilize/Calc2D";
import "./ControlElements.css";

// const handleWrapperTouchStart = (event) => {};
// const handleWrapperTouchMove = (event) => {};
// const handleWrapperTouchEnd = (event) => {};

function RenderCtrldoms() {
  // const wrapperRef = useRef(null)
  // const [styleElement, setStyleElement] = useState();

  // useEffect(() => {
  //   setEleStyle({
  //     eleTop: 0,
  //     eleLeft: 0,
  //     eleWidth: 150,
  //     eleHeight: 150,
  //     eleTransform: "none",
  //   })
  // },[])
  // wrapperRef.current &&  console.log(wrapperRef.current)


  render (
    // <div
    //   className="wrapper" ref={wrapperRef}
    //   onTouchStart={(event) => handleWrapperTouchStart(event)}
    //   onTouchMove={(event) => handleWrapperTouchMove(event)}
    //   onTouchEnd={(event) => handleWrapperTouchEnd(event)}
    // >
    //   <div className="item" style={setEleStyle(styleElement)}></div>
    // </div>

  )
}

export default RenderCtrldoms;
